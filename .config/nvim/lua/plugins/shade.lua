local shade = require("shade")

shade.setup({
  overlay_opacity = 40,
  opacity_step = 2,
  keys = {
    brightness_up = "<C-Up>",
    brightness_down = "<C-Down>",
    -- toggle = "<Leader>s",
  }
})
